info:
  name: oauth-protected-resources
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/.well-known/oauth-protected-resource"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
