info:
  name: get-question
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/v1/questions/{{question_id}}"
  auth: inherit

runtime:
  variables:
    - name: question_id
      value: 019d28d0-a543-740d-a65b-7e6694a67e10

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
