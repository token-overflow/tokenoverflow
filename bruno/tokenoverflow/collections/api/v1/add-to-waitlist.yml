info:
  name: add-to-waitlist
  type: http
  seq: 8

http:
  method: POST
  url: "{{base_url}}/v1/waitlist"
  body:
    type: json
    data: |-
      {}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
