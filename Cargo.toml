[workspace]
resolver = "2"
members = [
    "apps/api",
    "apps/embedding_service",
    "apps/so_tag_sync",
]

[profile.release]
opt-level = "s"
lto = "thin"
strip = "symbols"
panic = "abort"
codegen-units = 1
